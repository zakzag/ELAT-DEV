"use strict";

const baseUri = "/api";
const contactUri = "/contact";
const contactListUri = "/contact/list";
const logEntryUri = "/log";


module.exports = {
    BASE_URI: baseUri,
    CONTACT_URI: baseUri + contactUri,
    CONTACTLIST_URI: baseUri + contactListUri,
    LOGENTRY_URI: baseUri + logEntryUri
}